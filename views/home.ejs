

<%- include("partials/header") %>

    <% posts.forEach(function(post) { %>
        <h1><%= post.title %></h1>
        <p><%= post.body %></p>
    <% }); %>
        

<%- include("partials/footer") %>

